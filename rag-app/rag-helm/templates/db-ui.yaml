apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-ui
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-ui
  template:
    metadata:
      labels:
        app: db-ui
        project-name: wca4z
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        nvidia.com/gpu.present: "{{ .Values.nodeSelector.gpu }}"
      volumes:
        - name: rag-akm-storage-pvc
          persistentVolumeClaim:
            claimName: {{ .Values.pvc.name }}
      containers:
        - name: db-app
          image: {{ .Values.dbUI.image }}
          imagePullPolicy: IfNotPresent
          env:
            - name: HUGGINGFACE_HUB_CACHE
              value: "/platform_storage/HF_HOME"
            - name: PATH
              value: "/usr/bin:/bin:/usr/local/bin"
          ports:
            - name: websocket
              containerPort: {{ .Values.dbUI.port }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              echo "Starting DB UI"
              git clone https://github.com/vpatz/rag-llm-gitops-bharatgen.git
              cd rag-llm-gitops-bharatgen/rag-app
              python3 ui/ui_ingest.py
          resources:
{{ toYaml .Values.dbUI.resources | indent 12 }}
          volumeMounts:
{{ toYaml .Values.commonVolumeMount.volumeMounts | indent 12 }}
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: db-ui-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: db-ui
  ports:
    - name: websocket
      port: {{ .Values.dbUI.port }}
      targetPort: websocket
  type: ClusterIP

---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: db-ui-route
  namespace: {{ .Values.namespace }}
  annotations:
    haproxy.router.openshift.io/timeout: 5m
spec:
  host: db-ui.{{ .Values.routes.domain }}
  to:
    kind: Service
    name: db-ui-service
  port:
    targetPort: websocket
  tls:
    termination: edge
